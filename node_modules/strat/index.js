// This is used in the tests.  Some std components "require('strat')"
// and the tests take this module and assign mocks to it.

//Now here's a little lesson in trickery!
var x = () => undefined;
const setExport = function (fn) {
  x = fn;
  x.setExport = setExport;
  module.exports = x;
}
setExport(x);

